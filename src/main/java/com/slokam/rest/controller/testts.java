package com.slokam.rest.controller;

import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import com.slokam.rest.entity.Student;

@RestController
@RequestMapping("test")
public class testts {
	/*@GetMapping("/sudent")
	//public ResponseEntity<Student> getStudnt()
	{
		//Student st=new Student(1, "ramana", "inter", 3214L, 97);
	//	ResponseEntity<Student>re=new ResponseEntity<Student>(st, HttpStatus.OK);  //st=body,httpstatus=status
		//return re;
	}*/

}
